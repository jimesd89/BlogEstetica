@model Publicacion
@{
    ViewData["Title"] = Model.Titulo;
    ViewData["TitleHeader"] = Model.Titulo;
    ViewData["SubTitle"] = Model.Subtitulo;
    ViewData["Image"] = Model.Imagen1;
}

<section class="page-section">
    <div class="container">
        <div class="text-center">
            @Html.Raw(Model.Cuerpo)
        </div>
    </div>
</section>